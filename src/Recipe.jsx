import React from 'react';
import Header from './Header';

const Recipe = () => {
    return (
        <div>
            <Header />
            Recette
        </div>
    );
};

export default Recipe;
